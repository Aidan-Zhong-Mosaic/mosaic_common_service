apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashboard-auth-proxy.fullname" . }}
  labels:
    {{- include "dashboard-auth-proxy.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dashboard-auth-proxy.selectorLabels" . | nindent 4 }}
