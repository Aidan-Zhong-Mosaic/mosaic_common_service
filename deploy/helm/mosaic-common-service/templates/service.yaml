apiVersion: v1
kind: Service
metadata:
  name: {{ include "mosaic-common-service.fullname" . }}
  labels:
    {{- include "mosaic-common-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mosaic-common-service.selectorLabels" . | nindent 4 }}
