{
  "Comment": "Attach to a consuming service's execution role (e.g. mosaic-ai-chat's ECS task role or Lambda role). Grants ONLY the ability to invoke the gateway's API Gateway endpoint - it does NOT grant any access to Redshift or the gateway's local DB credentials file, which stay inside the gateway itself.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "InvokeMosaicCommonServices",
      "Effect": "Allow",
      "Action": "execute-api:Invoke",
      "Resource": "arn:aws:execute-api:eu-west-1:ACCOUNT_ID:API_ID/*/*/*"
    }
  ]
}
